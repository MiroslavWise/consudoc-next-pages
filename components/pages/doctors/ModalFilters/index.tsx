// import { useFilters } from "@/store/state/useFilters"

import styles from "./style.module.scss"

export const ModalFilters = () => {
    // const { isVisible, setIsVisible } = useFilters()

    return (
        <div className={styles.wrapper}>
            <div></div>
        </div>
    )
}
