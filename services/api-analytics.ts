// import { axiosInstance } from "./general-api"

export const apiAnalytics = async () => {
    // try {
    //     const usersAmount = await axiosInstance.get("/profiles/")
    //     const conferenceAmount = await axiosInstance.get("/conferences/")
    //     const doctorsAmount = await axiosInstance.get("/doctors-filter/")

    //     const data = {
    //         users: usersAmount?.data?.count,
    //         conferences: conferenceAmount?.data?.count,
    //         doctors: doctorsAmount?.data?.count,
    //     }

    //     return data
    // } catch (e) {
    //     console.error("ERROR ANALYTICS: ", e)
    // }
}
